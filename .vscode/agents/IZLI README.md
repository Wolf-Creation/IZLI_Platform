IZLI Workspace Agents

This folder contains local workspace copies of the agents used by the IZLI Platform.

They are presented in the VS Code Agents view when the scope is set to Workspace.

Copied from `.github/agents/` to ensure availability in the Agents UI.
